# Git Rules

## Commits

- Only commit when explicitly asked.
- Always request review of the commit message before committing files.
- Do not use structured commits formatting unless the repo is already using this convention
